# codealpha_PMTool
A project management tool built with Node.js backend and React frontend. It features user authentication, project creation, task assignment, and real-time notifications using Socket.io. The backend uses Express and SQLite for data storage, while the frontend provides an interactive dashboard for managing projects and tasks.
